apiVersion: v1
kind: Service
metadata:
  name: lab-selector-app
  namespace: uaim
spec:
  selector:
    app: lab-selector-app
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
